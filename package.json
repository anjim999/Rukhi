{
  "name": "auto-captions-production",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd backend && npm install --omit=dev",
    "build": "cd frontend && npm install && npm run build && cp -r dist/* ../",
    "start": "node index.js"
  },
  "dependencies": {}
}
